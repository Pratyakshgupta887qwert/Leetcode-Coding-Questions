class Solution {
    public boolean isMatch(String s, String p) {
        // RAG X (regular expression) is used for pattern matching.
        return s.matches(p);  // this methord is used to check that it maches the pattern


        

    }
}